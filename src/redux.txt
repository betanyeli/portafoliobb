redux-thunk
redux-devtools
react-redux

hacer un boilerplate de redux:
store, reducers, actions

store tiene index.js && initislState
initialState tiene
export default
lights:{
    red: true,
    yellow: false,
    green: false
}
index.js tiene
import createStore, applyMiddleware from 'redux'
import composeWithDevTools from 'redux-devtools-extension'
import thunkMiddleware from 'redux-thunk'
import reducer from 'reducers carpet
import state from initialState

actions tiene actionTypes.js && index.js && lights.js
reducers tiene index.js && lights.js

dispatch = manda cosas, es una f(x) de redux


Soy Betányeli. Desde que me titulé he buscado instancias donde desarrollar nuevos desafíos, y egresar de Laboratoria y conocer el mundo de la programación, fue uno de los más gratificantes retos. Constante, empática y altruista, trabajando constantemente para el logro de mis metas.